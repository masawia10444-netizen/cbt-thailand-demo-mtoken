version: '3.7'

services:
  webapp:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: next-cbt-webapp
    ports:
      - "1001:3001"
    env_file:
      - .env.production
    restart: always
